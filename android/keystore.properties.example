# Copie este arquivo para keystore.properties (mesma pasta) e preencha com
# os valores reais. keystore.properties NUNCA deve ser commitado — já está
# no .gitignore.
storeFile=C:/caminho/para/gpva-release.keystore
storePassword=senha-da-keystore
keyAlias=nome-do-alias
keyPassword=senha-da-chave
